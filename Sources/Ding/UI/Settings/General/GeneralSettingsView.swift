import SwiftUI

/// Placeholder view for general settings in Ding.
struct GeneralSettingsView: View {
    var body: some View {
        VStack {
            Text("General — TODO")
                .foregroundColor(.secondary)
                .font(.headline)
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
        .padding()
    }
}
